[project]
name = "magnesium-calcium-foundation"
version = "0.1.0"
description = "Mg/Ca foundation — electrolyte and light-alloy constraints."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
