[project]
name = "silicon-foundation"
version = "0.1.0"
description = "Silicon foundation — refining, device yield, thermal realism, screening."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
