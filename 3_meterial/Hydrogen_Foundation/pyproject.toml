[project]
name = "hydrogen-foundation"
version = "0.1.0"
description = "Hydrogen phenomenology foundation — production, storage, fuel cells, safety, screening."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
