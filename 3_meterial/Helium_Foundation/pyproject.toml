[project]
name = "helium-foundation"
version = "0.1.0"
description = "Helium phenomenology foundation — sourcing, cryogenic storage, safety, screening."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
