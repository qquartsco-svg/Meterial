[project]
name = "oxygen-foundation"
version = "0.1.0"
description = "Oxygen phenomenology — air separation, LOX, oxidiser safety, screening."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
