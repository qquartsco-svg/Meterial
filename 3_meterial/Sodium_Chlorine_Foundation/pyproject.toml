[project]
name = "sodium-chlorine-foundation"
version = "0.1.0"
description = "Sodium/chlorine foundation — chlor-alkali, salinity corrosion, screening."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
