[project]
name = "iron-foundation"
version = "0.1.0"
description = "Iron foundation — process decarbonization, corrosion realism, screening."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
