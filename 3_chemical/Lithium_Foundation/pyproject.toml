[project]
name = "lithium-foundation"
version = "0.1.0"
description = "Lithium foundation — extraction, battery chemistry coupling, screening."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
